{
  "name": "codette-e2e-tests",
  "version": "0.1.2",
  "license": "Apache-2.0",
  "private": true,
  "type": "module",
  "scripts": {
    "test": "npx playwright test --config tests/playwright.config.js",
    "test:headed": "npx playwright test --config tests/playwright.config.js --headed",
    "test:unit": "node --test 'server/src/**/*.test.js' 'client/src/**/*.test.js' 'host/*.test.js'"
  },
  "devDependencies": {
    "@playwright/test": "^1.52.0"
  },
  "dependencies": {
    "jose": "^6.2.3",
    "ws": "^8.21.0"
  }
}
