HOST_KEY=generate-with-openssl-rand-hex-32
# Externally-visible hostname (e.g. chat.example.com, or localhost:3000 for dev).
SERVER_HOSTNAME=chat.example.com
# HOST_TOKEN_TTL=2592000  # token lifetime in seconds (0 = never expires, 2592000 = 30 days)
